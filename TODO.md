# TODO

- [x] When the extension is opened from a non-Warp window and a tab is selected, focus the target Warp window after switching.
- [x] Fix the close-tab shortcut to use the actual Delete key instead of Backspace.
- [x] Show Claude Code state by tinting the tab icon in a Claude-colored style, similar to active tabs, instead of using a Claude Code banner.
